# 主页每日寄语设计

## 目标

移除主页顶部的 “PERSONAL SPACE” 和 “我的主页” 大标题，仅保留一条简洁的每日寄语及作者出处。

## 方案

- 在前端维护一组精选中文寄语，不依赖网络服务。
- 使用本地日期计算当天索引；同一天内容稳定，日期变化后自动换一句。
- 顶部继续保留“编辑工作台”按钮，避免影响现有卡片布局编辑能力。
- 寄语使用普通正文尺寸，作者作为次要文字显示，降低视觉重量。

## 边界与验证

- 不新增数据库和接口，不收集用户数据。
- 单元测试固定日期，验证隐藏旧标题、显示当日寄语与作者、保留编辑按钮。
- 运行页面测试、类型检查和正式构建。
