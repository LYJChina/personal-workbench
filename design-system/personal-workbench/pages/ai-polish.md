# AI 润色页面覆盖

AI 办公首页避免重复的宣传型 Hero，只保留页面标题、简短说明和可用工具列表，使核心入口保持在首屏。

AI 润色工作区采用两级卡片层次：顶部四张场景卡片负责切换任务；下方左右双栏分别显示素材表单与生成结果。选中场景必须同时使用边框、背景和 `aria-current` 表达，不能只依赖颜色。场景写入查询参数以支持刷新和深链接。

提示词默认展开、可见且可编辑，并提供恢复默认操作。历史区域使用带可访问选中状态的场景标签筛选；移动端按单列排列，所有点击区域至少 44px，不能产生横向滚动。
