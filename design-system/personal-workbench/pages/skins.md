# 外观工作室页面覆盖

本页覆盖旧版全局“固定青绿色”方向。界面采用可切换皮肤架构，三套皮肤分别为冰羽蓝、雾林青和星河夜。每套皮肤使用项目内的原创 WebP 风景背景，并独立定义遮罩、文字对比度、边框、强调色和玻璃表面；默认使用明亮、低干扰的冰羽蓝。

组件必须使用语义变量，不得依赖某一套皮肤的原始色值。玻璃开启时，主要导航、卡片、表单与状态组件使用 18–20px 背景模糊和半透明表面；关闭时切换为高不透明度表面。外观设置即时预览并仅在本地持久化。交互控件保持可见标签、键盘焦点与至少 44px 的移动端点击区域。
